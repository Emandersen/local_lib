extends layout 

block content 
    h1= genre.name
    ul
        each book, i in genre_books
            li
                a(href='/book/#{book.id}')= book.title
                p Written by: #{book.author}
                p Summary: #{book.summary}
                p ISBN: #{book.isbn}
                
    